See README for installation requirements.

If the requirements are fulfilled, installation is simple:

$ python setup.py install [--user]

With --user, the files are copied to ~/.local instead of the system-wide
default location (probably /usr). Make sure to add ~/.local/bin to PATH.

